import React from "react";
import ReactDOM from "react-dom";
import { ApproverFinder } from './components/ApproverFinder';

const rootElement = document.getElementById("root");
ReactDOM.render(<ApproverFinder />, rootElement);
